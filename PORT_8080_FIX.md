# 🔴 ПРОБЛЕМА: Порт 8080 уже занят

## ❌ Ошибка:

```
***************************
APPLICATION FAILED TO START
***************************

Description:

Web server failed to start. Port 8080 was already in use.

Action:

Identify and stop the process that's listening on port 8080 
or configure this application to listen on another port.
```

---

## 🔍 Причина проблемы:

**Другой процесс Java уже использует порт 8080.**

Это происходит потому, что:
1. Вы ранее запустили приложение (возможно, через IntelliJ IDEA)
2. Процесс не был корректно остановлен
3. Теперь порт 8080 занят старым процессом
4. Новый экземпляр приложения не может запуститься на том же порту

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Автоматическое решение (Рекомендуется)

Просто дважды кликните на файл **`start.bat`** в корне проекта.

Этот скрипт автоматически:
- ✅ Найдет процесс на порту 8080
- ✅ Убьет его
- ✅ Запустит приложение заново

### Вариант 2: Ручное решение через PowerShell

```powershell
# Найти процесс на порту 8080
netstat -ano | Select-String ":8080"

# Убить процесс (замените PID на актуальный номер процесса)
taskkill /F /PID <PID>

# Запустить приложение
cd C:\jakarta\university
.\mvnw.cmd spring-boot:run
```

### Вариант 3: Через одну команду PowerShell

```powershell
netstat -ano | Select-String ":8080" | ForEach-Object { 
    if ($_ -match '\s+(\d+)$') { 
        taskkill /F /PID $matches[1] 
    } 
}
```

### Вариант 4: Изменить порт приложения

Отредактируйте `src/main/resources/application.yml`:

```yaml
server:
  port: 8081  # Измените на свободный порт
```

Затем запустите приложение. Оно будет доступно по адресу:
- http://localhost:8081/

---

## 🚀 После решения проблемы:

Приложение успешно запустится и вы увидите в логах:

```
:: Spring Boot ::                (v3.4.0)

...
INFO ... : Tomcat initialized with port 8080 (http)
...
INFO ... : Tomcat started on port 8080 (http)
INFO ... : Started Application in X.XXX seconds
```

### Доступные URL:

| URL | Описание |
|-----|----------|
| http://localhost:8080/ | 🌐 Главная страница |
| http://localhost:8080/api-docs | 📖 REST API Документация |
| http://localhost:8080/dashboard | 📊 Панель управления |
| http://localhost:8080/api/main | 🚀 REST API эндпоинт |
| http://localhost:8080/h2-console | 🗄️ H2 Database Console |

---

## 🔧 Дополнительные команды:

### Проверить, какой процесс использует порт:

```powershell
netstat -ano | Select-String ":8080"
```

Вывод:
```
TCP    0.0.0.0:8080    0.0.0.0:0    LISTENING    6736
                                                  ^^^^
                                                  PID процесса
```

### Узнать, что за процесс:

```powershell
Get-Process -Id 6736 | Select-Object ProcessName, Id
```

### Убить все Java процессы:

```powershell
taskkill /F /IM java.exe
```

⚠️ **ВНИМАНИЕ:** Это убьет ВСЕ Java процессы на компьютере!

---

## 📋 Проверка работоспособности:

После запуска приложения выполните:

```powershell
# Проверка HTTP запросом
Invoke-WebRequest http://localhost:8080/api/main -UseBasicParsing
```

Ожидаемый результат:
```
StatusCode: 200
Content: Hello World! Система управления университетом работает.
```

---

## 💡 Советы:

1. **Всегда останавливайте приложение перед повторным запуском**
   - В IntelliJ IDEA: нажмите красную кнопку "Stop"
   - В терминале: нажмите `Ctrl+C`

2. **Используйте start.bat для запуска**
   - Он автоматически очищает порт
   - Не нужно вручную убивать процессы

3. **Проверяйте порт перед запуском**
   ```powershell
   netstat -ano | Select-String ":8080"
   ```
   Если ничего не возвращается - порт свободен ✅

4. **В разработке можно использовать разные порты**
   - Основное приложение: 8080
   - Тестовое окружение: 8081
   - Debug версия: 8082

---

## ✅ ИТОГ:

**Проблема решена!** Порт 8080 освобожден, приложение теперь может запуститься.

**Используйте `start.bat` для быстрого запуска без проблем!**

---

**Дата:** 2026-02-14  
**Решение:** Автоматическая очистка порта в start.bat  
**Статус:** ✅ Исправлено

