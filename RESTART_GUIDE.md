# 🚀 Быстрый перезапуск приложения

## Проблема решена!

Порт 8080 был занят предыдущим процессом. Теперь он освобожден.

## 3 способа запуска приложения:

### 1️⃣ Через bat-файл (САМЫЙ ПРОСТОЙ)
Просто дважды кликните на файл:
```
restart-app.bat
```

Он автоматически:
- ✅ Остановит процессы на порту 8080
- ✅ Настроит окружение Java
- ✅ Запустит Spring Boot приложение

### 2️⃣ Через IntelliJ IDEA
1. Откройте файл `Application.java`
2. Нажмите зеленую стрелку ▶️ рядом с `public static void main`
3. Если ошибка "Port 8080" - остановите предыдущий запуск (красный квадрат ⬛)
4. Запустите снова

### 3️⃣ Через PowerShell
```powershell
# Остановить процессы на порту 8080
$processes = netstat -ano | Select-String ":8080" | Select-String "LISTENING"
foreach ($line in $processes) {
    if ($line -match '\s+(\d+)\s*$') {
        Stop-Process -Id $Matches[1] -Force
    }
}

# Запустить приложение
$env:JAVA_HOME="C:\Program Files\Java\jdk-24"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
cd C:\jakarta\university
.\mvnw.cmd spring-boot:run
```

## ✅ После запуска откройте браузер:

```
http://localhost:8080/
```

## 🔑 Войдите в систему:

### Администратор:
- Email: `admin@university.kz`
- Пароль: `admin123`

### Студент:
- Email: `asel@student.kz`
- Пароль: `123456`

---

## 💡 Почему это происходит?

Ошибка "Port 8080 already in use" возникает когда:
1. Вы закрыли IntelliJ IDEA, но Java процесс продолжил работать
2. Запустили приложение дважды
3. Другая программа использует порт 8080

## 🛠️ Постоянное решение:

Используйте файл **`restart-app.bat`** для запуска - он всегда остановит старые процессы перед запуском!

---

**Готово! Приложение работает! 🎉**

