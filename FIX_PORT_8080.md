# 🔧 Решение проблемы "Port 8080 already in use"

## ❌ Ошибка
```
Web server failed to start. Port 8080 was already in use.
```

## ✅ Решение

### Автоматическое решение (один скрипт)
Скопируйте и выполните в PowerShell:

```powershell
# Остановить все процессы на порту 8080
$processes = netstat -ano | Select-String ":8080" | Select-String "LISTENING"
if ($processes) {
    foreach ($line in $processes) {
        $pidMatch = $line -match '\s+(\d+)$'
        if ($pidMatch) {
            $processId = $Matches[1]
            Write-Host "Остановка процесса $processId..."
            Stop-Process -Id $processId -Force -ErrorAction SilentlyContinue
        }
    }
    Write-Host "✅ Порт 8080 освобожден!"
} else {
    Write-Host "✅ Порт 8080 уже свободен"
}

# Подождать 2 секунды
Start-Sleep -Seconds 2

# Запустить приложение
$env:JAVA_HOME="C:\Program Files\Java\jdk-24"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
cd C:\jakarta\university
.\mvnw.cmd spring-boot:run
```

### Пошаговое решение

#### Шаг 1: Найти процесс на порту 8080
```powershell
netstat -ano | Select-String ":8080" | Select-String "LISTENING"
```

Вывод будет примерно таким:
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING       23424
```

Последнее число (23424) - это **PID процесса**.

#### Шаг 2: Остановить процесс
```powershell
Stop-Process -Id 23424 -Force
```
Замените `23424` на ваш PID из шага 1.

#### Шаг 3: Проверить освобождение порта
```powershell
netstat -ano | Select-String ":8080" | Select-String "LISTENING"
```
Если ничего не вывелось - порт свободен!

#### Шаг 4: Запустить приложение
```powershell
$env:JAVA_HOME="C:\Program Files\Java\jdk-24"
$env:Path="$env:JAVA_HOME\bin;$env:Path"
cd C:\jakarta\university
.\mvnw.cmd spring-boot:run
```

## 🔍 Почему это происходит?

1. **Предыдущий запуск не завершился** - вы закрыли IDE, но Java процесс продолжил работать
2. **Несколько экземпляров** - запустили приложение дважды
3. **Другое приложение** - порт 8080 используется другой программой

## 🛠️ Альтернативные решения

### Вариант 1: Изменить порт
Если не хотите останавливать процесс, измените порт в `application.yml`:

```yaml
server:
  port: 8081  # Вместо 8080
```

### Вариант 2: Остановить все Java процессы
```powershell
Get-Process | Where-Object {$_.ProcessName -eq "java"} | Stop-Process -Force
```
⚠️ **Внимание**: Это остановит ВСЕ Java приложения!

### Вариант 3: Использовать другой терминал
Запустите приложение через IntelliJ IDEA:
1. Откройте `Application.java`
2. Нажмите зеленую стрелку ▶️
3. Если ошибка - остановите предыдущий запуск (красный квадрат ⬛)

## 📋 Быстрая диагностика

### Проверить, запущено ли приложение
```powershell
$port = netstat -ano | Select-String ":8080" | Select-String "LISTENING"
if ($port) { 
    Write-Host "✅ Приложение работает на порту 8080" 
} else { 
    Write-Host "❌ Приложение не запущено" 
}
```

### Проверить в браузере
Откройте: http://localhost:8080/

Если видите главную страницу - приложение работает!

## 🎯 Тестовые аккаунты

После успешного запуска войдите в систему:

### Администратор
- Email: `admin@university.kz`
- Пароль: `admin123`
- URL: http://localhost:8080/auth/login

### Студент
- Email: `asel@student.kz`
- Пароль: `123456`
- URL: http://localhost:8080/auth/login

## 💡 Совет

Создайте bat-файл для быстрого перезапуска:

**restart.bat**
```batch
@echo off
echo Остановка процессов на порту 8080...
for /f "tokens=5" %%a in ('netstat -ano ^| findstr :8080 ^| findstr LISTENING') do taskkill /F /PID %%a >nul 2>&1
timeout /t 2 >nul
echo Запуск приложения...
set JAVA_HOME=C:\Program Files\Java\jdk-24
set PATH=%JAVA_HOME%\bin;%PATH%
cd /d C:\jakarta\university
call mvnw.cmd spring-boot:run
```

Просто запустите `restart.bat` и всё заработает!

---

**Проблема решена! ✅**

Теперь откройте браузер: **http://localhost:8080/**

