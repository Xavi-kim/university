# 🔐 Данные для входа в систему

## ✅ Учетные данные:

```
Username: admin
Password: admin123
```

---

## 🌐 Где использовать:

После запуска приложения откройте в браузере любой из URL:

### С context path `/university` (текущая конфигурация):
- http://localhost:8080/university/
- http://localhost:8080/university/dashboard
- http://localhost:8080/university/api-docs
- http://localhost:8080/university/h2-console

### После перезапуска (без `/university`):
- http://localhost:8080/
- http://localhost:8080/dashboard
- http://localhost:8080/api-docs
- http://localhost:8080/h2-console

---

## 👤 Роли пользователя admin:

Пользователь **admin** имеет все роли:
- ✅ **ADMIN** - полный административный доступ
- ✅ **PROFESSOR** - доступ к управлению курсами и пользователями
- ✅ **STUDENT** - доступ к просмотру курсов

---

## 🔑 Разрешения:

| Ресурс | Доступ |
|--------|--------|
| **Главная страница** (/) | ✅ Публичный доступ |
| **API Docs** (/api-docs) | ✅ Публичный доступ |
| **API Main** (/api/main) | ✅ Публичный доступ |
| **Курсы** (/api/courses) | ✅ Доступ (роль STUDENT+) |
| **Пользователи** (/api/users) | ✅ Доступ (роль PROFESSOR+) |
| **Dashboard** (/dashboard) | ✅ Доступ (авторизованные) |
| **H2 Console** (/h2-console) | ✅ Доступ (роль ADMIN) |

---

## 📋 Инструкция по входу:

1. **Запустите приложение** через `start.bat` или IntelliJ IDEA

2. **Откройте браузер** и перейдите по адресу:
   ```
   http://localhost:8080/university/dashboard
   ```

3. **Введите данные:**
   - Username: `admin`
   - Password: `admin123`

4. **Нажмите "Sign in"** или "Войти"

5. **Готово!** Вы вошли в систему с полным доступом

---

## 🔄 Как изменить пароль:

Если хотите изменить пароль, отредактируйте файл:
```
src/main/java/org/example/university/config/SecurityConfig.java
```

Найдите строку:
```java
.password(passwordEncoder().encode("admin123"))
```

И замените `"admin123"` на ваш новый пароль.

---

## ⚠️ Важно:

- Эти данные используются **только для разработки**
- В production окружении используйте:
  - Надежные пароли
  - Шифрование
  - Базу данных для хранения пользователей
  - Двухфакторную аутентификацию

---

## 🚀 Быстрый старт:

```powershell
# 1. Перейдите в папку проекта
cd C:\jakarta\university

# 2. Запустите приложение
.\start.bat

# 3. Откройте браузер
# http://localhost:8080/university/

# 4. Войдите с данными:
# admin / admin123
```

---

**Дата создания:** 2026-02-14  
**Пользователь:** admin  
**Пароль:** admin123  
**Статус:** ✅ Активен

